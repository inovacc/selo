# Implementation Plans

Generated by the `improve` skill on 2026-06-18, against commit `15c0c91`
(branch `feat/complete-toolkit`, repo `github.com/inovacc/selo`).

Each executor: read the plan fully before starting, honor its STOP conditions, run every
verification command, and update your row below when done. Plans are self-contained — you
do not need any other plan or prior context to execute one.

**Repo verification commands** (used as gates in every plan): `go build ./...`,
`go vet ./...`, `go test ./...` (full) / `go test -short ./...` (fast), `gofmt -l <files>`,
`golangci-lint run` (config: `.golangci.yml`; may need install). Conventional-commit messages.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI actually gates `main` (+ drop GUI cruft / fix failing build.yml) | P1 | S | — | DONE — re-applied to current main as `fc0fbdb` (stale pre-rebrand worktree NOT merged); both workflows now run on every branch + PR incl. main; GUI apt-get step removed; Actions enabled live |
| 002 | CNPJ rejects all-equal inputs (e.g. `00000000000000`), matching CPF | P1 | S | — | DONE (merged to main; reused `allEqualBytes`) |
| 003 | Finish rebrand — `brdoc mcp:` error prefix → `selo mcp:` | P3 | S | — | DONE (merged to main `5ad8d4d`; executor commit `2a00dff`; leftover scan clean) |
| 004 | Verify RG (SP/RJ) check-digit convention vs authoritative sources | P2 | M | — | DONE — convention **A verified correct** (commit `7fbc546`); 4 independent sources agree + algebraic-equivalence proof; samples pinned in `TestRG_AuthoritativeSamples`. Residual: RJ shares SP algo but unverified (see BACKLOG) |
| 005 | Split `brdoc.go` god-file into `cpf.go` + `cnpj.go` (shed stale brand name) | P3 | M | — | DONE (merged to main `6e0ca27`; executor's first commit omitted the cpf.go deletion — controller completed it as `7246fab`; committed blobs gofmt-clean, suite green) |
| 006 | (design/spike) Inscrição Estadual — architecture + verified first-batch UFs | P2 | L | 004 (precedent) | DONE — SP shipped + verified (commit `d41dce1`); `ie.go`/`ie_test.go`, CLI/MCP auto-derive; MG/RJ/RS/PR deferred (no ≥2 samples); roadmap in `docs/IE-NOTES.md` |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale).

## Recommended order & dependency notes
- **001 first.** It re-enables CI and makes `main` PRs (incl. PR #3) actually run tests+lint+vulncheck. Once it lands, plans 002–004 are automatically validated by CI on push. None of 002–004 *hard*-depend on 001, but landing 001 first is the highest-leverage move.
- **002 / 003** are independent quick wins; either order.
- **004 must precede any future "multi-state RG" feature** (audit direction D2) — don't build 20+ more per-UF RG algorithms on an unverified check-digit convention.
- **005** is a behavior-preserving file move (lowest priority); land it after 001 so CI validates the refactor. It does not block anything.

## Finding → plan mapping
- Finding #1 (PR→`main` has no CI) → **001**
- Finding #3 (CI installs GUI/audio cruft) → folded into **001** (same files)
- Finding #2 (CNPJ accepts all-zeros) → **002**
- Finding #4 (`brdoc mcp:` rebrand leftover) → **003**
- Finding #5 (RG check-digit convention unverified) → **004**
- Finding #6 (`brdoc.go` god-file + stale filename) → **005**

## Findings considered this round, not planned
(Recorded so they aren't re-audited from scratch next run. Not "rejected" — just lower leverage than the four above; promote any of these in a future `improve` run.)

- **#7 — `scanBuf` shared package-level buffer** (`cmd/selo/iohelper.go:15`) reused by every `streamValidate`. Latent data race only if ever called concurrently; the CLI is serial today. One-line fix (make the buffer call-local) — fold into the next change that touches `iohelper.go`; not worth a standalone plan now.
- **#8 — MCP error-path coverage (84.4%)**: `errResult` branches in handlers largely untested. Low risk; the reusable CI workflow already lints + vulnchecks. Revisit if the `mcp` package grows.
- **#9 — minor cleanups**: hand-rolled insertion sort for DDDs (`phone.go:51`; `sort.Ints` exists) and two unbounded `for{}` retry loops in `person.go` (192, 204) that provably terminate but lack a defensive bound. Cosmetic/micro; bundle opportunistically.

## Direction options (separate from the table above — maintainer's call, not bugs)
Grounded in `docs/BACKLOG.md` and the code. Selected ones become *design/spike* plans, not build-everything plans.
- **D1 — Inscrição Estadual (per-UF)**: biggest open gap in the Go ecosystem (paemuri/brdoc #7). 27 per-UF algorithms behind the existing `UFScoped` pattern. → **planned as 006** (design/spike: architecture + verified first batch + per-UF research handoff).
- **D2 — Multi-state RG**: extend `rg.go` beyond SP/RJ; unblocks `Person.RG` for all states. **Do plan 004 first.**
- **D3 — `GenPerson` `WithSeed`**: generators use the non-seedable global RNG; deterministic fixtures need them to accept a `*rand.Rand` source (a cross-cutting generator refactor, useful for all types).

To turn any of these into a plan, run `/improve plan <description>` or `/improve next`.
