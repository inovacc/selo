// Code generated by selo gen --lang java. DO NOT EDIT.
package com.inovacc.selo;

import java.util.Random;

/** Renavam validates and formats a Brazilian RENAVAM. */
public final class Renavam {
    private Renavam() {
    }

    private static final Mod11.CheckDigit DV = new Mod11.CheckDigit(new int[]{3, 2, 9, 8, 7, 6, 5, 4, 3, 2}, Mod11.DVRule.MOD_REMAINDER, new int[]{10}, true, 0, 0);

    /** validate reports whether value is a valid 11-digit RENAVAM. */
    public static boolean validate(String value) {
        String d = Mod11.onlyDigits(value);
        if (d.length() != 11) {
            return false;
        }
        if (Mod11.allEqual(d)) {
            return false;
        }
        int[] digits = Mod11.digits(d);
        int dv = Mod11.computeDigit(Mod11.weightedSum(Mod11.slice(digits, 0, 10), DV.weights), DV);
        return dv == digits[10];
    }

    /** format left-pads shorter inputs to 11 digits (no separator mask). */
    public static String format(String value) {
        String d = Mod11.onlyDigits(value);
        if (d.length() < 11) {
            StringBuilder sb = new StringBuilder();
            for (int i = 0; i < 11 - d.length(); i++) {
                sb.append('0');
            }
            d = sb + d;
        }
        return d;
    }

    /** generate returns a random valid 11-digit RENAVAM. */
    public static String generate() {
        Random rng = new Random();
        String out;
        do {
            int[] d = new int[10];
            for (int i = 0; i < 10; i++) {
                d[i] = rng.nextInt(10);
            }
            int dv = Mod11.computeDigit(Mod11.weightedSum(d, DV.weights), DV);
            StringBuilder sb = new StringBuilder();
            for (int v : d) {
                sb.append(v);
            }
            sb.append(dv);
            out = sb.toString();
        } while (Mod11.allEqual(out));
        return out;
    }
}
