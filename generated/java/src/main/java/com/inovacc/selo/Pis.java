// Code generated by selo gen --lang java. DO NOT EDIT.
package com.inovacc.selo;

/** Pis validates and formats this single-check-digit numeric document. */
public final class Pis {
    private Pis() {
    }

    private static final Mod11.CheckDigit DV = new Mod11.CheckDigit(new int[]{3, 2, 9, 8, 7, 6, 5, 4, 3, 2}, Mod11.DVRule.MOD_REMAINDER, new int[]{0, 1}, false, 0, 0);

    /** validate reports whether value is a valid Pis. */
    public static boolean validate(String value) {
        String d = Mod11.onlyDigits(value);
        if (d.length() != 11) {
            return false;
        }
        if (Mod11.allEqual(d)) {
            return false;
        }
        int[] digits = Mod11.digits(d);
        int dv = Mod11.computeDigit(Mod11.weightedSum(Mod11.slice(digits, 0, 10), DV.weights), DV);
        return dv == digits[10];
    }

    /** format renders the canonical mask, or throws on bad length. */
    public static String format(String value) {
        String d = Mod11.onlyDigits(value);
        if (d.length() != 11) {
            throw new IllegalArgumentException("ErrInvalidLength");
        }
        return d.substring(0, 3) + "." + d.substring(3, 8) + "." + d.substring(8, 10) + "-" + d.substring(10, 11);
    }
}
