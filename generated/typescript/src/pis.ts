// Code generated by selo gen --lang ts. DO NOT EDIT.

import {
  type CheckDigit,
  charValue,
  weightedSum,
  computeDigit,
  encodeDigit,
  onlyDigits,
  allEqual,
} from "./mod11.js";

const DV: CheckDigit = { weights: [3, 2, 9, 8, 7, 6, 5, 4, 3, 2], rule: "mod_remainder", remainderTo0: [0, 1] };

/** validatePIS reports whether value is a valid PIS. */
export function validatePIS(value: string): boolean {
  const d = onlyDigits(value);
  if (d.length !== 11) return false;
  if (allEqual(d)) return false;
  const digits = d.split("").map(Number);
  const dv = computeDigit(weightedSum(digits.slice(0, 10), DV.weights), DV);
  return dv === digits[10];
}

/** formatPIS renders the canonical mask, or throws on bad length. */
export function formatPIS(value: string): string {
  const d = onlyDigits(value);
  if (d.length !== 11) throw new Error("ErrInvalidLength");
  return `${d.slice(0, 3)}.${d.slice(3, 8)}.${d.slice(8, 10)}-${d.slice(10, 11)}`;
}
