// Code generated by selo gen --lang ts. DO NOT EDIT.

import {
  type CheckDigit,
  charValue,
  weightedSum,
  computeDigit,
  encodeDigit,
  onlyDigits,
  allEqual,
} from "./mod11.js";

const DV: CheckDigit = { weights: [3, 2, 9, 8, 7, 6, 5, 4, 3, 2], rule: "mod_remainder", remainderTo0: [10], multiplyBy10: true };

/** validateRenavam reports whether value is a valid 11-digit RENAVAM. */
export function validateRenavam(value: string): boolean {
  const d = onlyDigits(value);
  if (d.length !== 11) return false;
  if (allEqual(d)) return false;
  const digits = d.split("").map(Number);
  const dv = computeDigit(weightedSum(digits.slice(0, 10), DV.weights), DV);
  return dv === digits[10];
}

/** formatRenavam left-pads shorter inputs to 11 digits (no separator mask). */
export function formatRenavam(value: string): string {
  let d = onlyDigits(value);
  if (d.length < 11) d = "0".repeat(11 - d.length) + d;
  return d;
}
