# Code generated by selo gen --lang python. DO NOT EDIT.

from __future__ import annotations

import random

from .mod11 import weighted_sum, compute_digit, only_digits, all_equal

_DV = {"weights": [3, 2, 9, 8, 7, 6, 5, 4, 3, 2], "rule": "mod_remainder", "remainder_to0": [0, 1]}


def validate_pis(value: str) -> bool:
    """Report whether value is a valid pis."""
    d = only_digits(value)
    if len(d) != 11:
        return False
    if all_equal(d):
        return False
    digits = [int(c) for c in d]
    dv = compute_digit(weighted_sum(digits[0:10], _DV["weights"]), _DV)
    return dv == digits[10]


def format_pis(value: str) -> str:
    """Render the canonical mask, or raise on bad length."""
    d = only_digits(value)
    if len(d) != 11:
        raise ValueError("ErrInvalidLength")
    return f"{d[0:3]}.{d[3:8]}.{d[8:10]}-{d[10:11]}"


def generate_pis() -> str:
    """Return a random, valid pis (unformatted)."""
    while True:
        b = [random.randint(0, 9) for _ in range(10)]
        b.append(compute_digit(weighted_sum(b, _DV["weights"]), _DV))
        out = "".join(str(x) for x in b)
        if not all_equal(out):
            return out
