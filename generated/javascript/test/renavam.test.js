// Code generated by selo gen --lang js. DO NOT EDIT.

import { describe, it, expect } from "vitest";
import { validateRenavam, formatRenavam, generateRenavam } from "../src/renavam.js";
import vector from "../vectors/renavam.json" assert { type: "json" };

const v = vector;

describe("renavam", () => {
  describe("validate", () => {
    for (const c of v.validate) {
      it(`validate ${JSON.stringify(c.input)} -> ${c.valid}`, () => {
        expect(validateRenavam(c.input)).toBe(c.valid);
      });
    }
  });

  describe("format", () => {
    for (const c of v.format) {
      it(`format ${JSON.stringify(c.input)}`, () => {
        if (c.error !== undefined) {
          expect(() => formatRenavam(c.input)).toThrow();
        } else {
          expect(formatRenavam(c.input)).toBe(c.output);
        }
      });
    }
  });

  describe("generate", () => {
    it("round-trip: 100 generated values validate", () => {
      for (let i = 0; i < 100; i++) {
        expect(validateRenavam(generateRenavam())).toBe(true);
      }
    });
  });
});
