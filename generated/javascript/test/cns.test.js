// Code generated by selo gen --lang js. DO NOT EDIT.

import { describe, it, expect } from "vitest";
import { validateCNS, formatCNS } from "../src/cns.js";
import vector from "../vectors/cns.json" assert { type: "json" };

const v = vector;

describe("cns", () => {
  describe("validate", () => {
    for (const c of v.validate) {
      it(`validate ${JSON.stringify(c.input)} -> ${c.valid}`, () => {
        expect(validateCNS(c.input)).toBe(c.valid);
      });
    }
  });

  describe("format", () => {
    for (const c of v.format) {
      it(`format ${JSON.stringify(c.input)}`, () => {
        if (c.error !== undefined) {
          expect(() => formatCNS(c.input)).toThrow();
        } else {
          expect(formatCNS(c.input)).toBe(c.output);
        }
      });
    }
  });
});
