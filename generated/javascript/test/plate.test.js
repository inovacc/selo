// Code generated by selo gen --lang js. DO NOT EDIT.

import { describe, it, expect } from "vitest";
import { validatePlate, formatPlate } from "../src/plate.js";
import vector from "../vectors/plate.json" assert { type: "json" };

const v = vector;

describe("plate", () => {
  describe("validate", () => {
    for (const c of v.validate) {
      it(`validate ${JSON.stringify(c.input)} -> ${c.valid}`, () => {
        expect(validatePlate(c.input)).toBe(c.valid);
      });
    }
  });

  describe("format", () => {
    for (const c of v.format) {
      it(`format ${JSON.stringify(c.input)}`, () => {
        if (c.error !== undefined) {
          expect(() => formatPlate(c.input)).toThrow();
        } else {
          expect(formatPlate(c.input)).toBe(c.output);
        }
      });
    }
  });
});
